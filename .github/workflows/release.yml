name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write
  actions: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: l-ra/knowledge-itmap
  MCP_IMAGE_NAME: l-ra/knowledge-itmap-mcp
  CHART_PATH: deploy/helm/knowledge-itmap

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: ver
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Set chart version
        run: ./deploy/helm/scripts/set-chart-version.sh "${{ steps.ver.outputs.version }}"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Test
        run: |
          npm ci
          npm test
          npm run build
          npm run mcp:build

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # QEMU: UI final stage runs `apk add gettext` on linux/arm64.
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=knowledge-itmap
          cache-to: type=gha,mode=max,scope=knowledge-itmap

      - uses: docker/metadata-action@v5
        id: meta-mcp
        with:
          images: ${{ env.REGISTRY }}/${{ env.MCP_IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.mcp
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-mcp.outputs.tags }}
          labels: ${{ steps.meta-mcp.outputs.labels }}
          cache-from: type=gha,scope=knowledge-itmap-mcp
          cache-to: type=gha,mode=max,scope=knowledge-itmap-mcp

      - uses: azure/setup-helm@v4
        with:
          version: v3.16.4

      - name: Package and push Helm chart
        id: chart
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          helm package "${{ env.CHART_PATH }}" --destination /tmp/chart
          CHART_ARCHIVE="/tmp/chart/knowledge-itmap-${VERSION}.tgz"
          echo "archive=${CHART_ARCHIVE}" >> "$GITHUB_OUTPUT"
          helm push "${CHART_ARCHIVE}" oci://${{ env.REGISTRY }}/l-ra

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.chart.outputs.archive }}
          generate_release_notes: true

      - name: Sync Chart.yaml on main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout main
          ./deploy/helm/scripts/set-chart-version.sh "${{ steps.ver.outputs.version }}"
          git add "${{ env.CHART_PATH }}/Chart.yaml"
          if git diff --staged --quiet; then
            echo "Chart.yaml already at ${{ steps.ver.outputs.version }} on main"
          else
            git commit -m "chore(helm): set chart version to ${{ steps.ver.outputs.version }} [skip ci] [skip release]"
            git push origin main
          fi
